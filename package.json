{
  "name": "threejs-screen-input-manager",
  "version": "1.0.0",
  "description": "A touch and mouse manager for threejs.",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && tsc && cp package.json README.md dist/ ",
    "clean": "rm -rf ./dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.7.10",
    "@types/three": "^0.131.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "three": "^0.131.3"
  }
}
